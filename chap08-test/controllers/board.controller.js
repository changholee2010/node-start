// 컨트롤러 영역.
const svc = require("../services/board.service");

const ctrl = {
  list: async (req, res) => {},
  create: async (req, res) => {},
  remove: async (req, res) => {},
};

module.exports = ctrl;
